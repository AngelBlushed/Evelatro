node_modules/
dist/
build/
.app-data/

# Sources hors-jeu (images CS:GO brutes, notes perso) — pas dans le dépôt
/IMAGE CS GO/
/autre made by eve not claude/

# Version .exe (générée par npm run build)
/EveLatro/
/EveLatro.exe
/EveLatro-Demo.apk

# Version dev (générée par npm run build:dev) — JAMAIS dans le zip
/dev/
/.dev-build/

# Android / Capacitor (générés)
/android/app/build/
/android/build/
/android/.gradle/
/android/local.properties
/android/app/src/main/assets/public/
/android/app/src/main/assets/capacitor.config.json
/www/

# APK générés (vont sur GitHub Releases via npm run release)
/EveLatro.apk
/EveLatro*.apk
/site/EveLatro.apk

# site/play est régénéré par npm run site:play — pas besoin dans le repo
/site/play/

# pipeline de génération des cartes TGC (outils + gros fichiers sources) — pas dans le repo
/tgc/

# logs du bot quand il tourne en local (LANCER-LE-BOT.bat)
/bot/*.log
